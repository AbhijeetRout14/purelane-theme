<!doctype html>
<html lang="en">
  <head>
    {{ content_for_header }}
    {% render 'purelane-head' %}
  </head>
  <body>
    {{ content_for_layout }}
    {% render 'purelane-footer' %}
  </body>
</html>
